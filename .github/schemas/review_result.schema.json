{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/Rajath2005/SmartCityApp/schemas/review_result",
  "title": "AI Review Result",
  "description": "Schema for validating AI-generated pull request review output. The AI review engine must return JSON matching this schema.",
  "type": "object",
  "required": ["decision", "summary", "score", "comments"],
  "properties": {
    "decision": {
      "type": "string",
      "enum": ["APPROVE", "REQUEST_CHANGES", "COMMENT"],
      "description": "The review verdict determining the GitHub review action."
    },
    "summary": {
      "type": "string",
      "minLength": 10,
      "maxLength": 500,
      "description": "Human-readable summary of the review (1-2 sentences)."
    },
    "score": {
      "type": "integer",
      "minimum": 0,
      "maximum": 100,
      "description": "Quality score from 0 (worst) to 100 (best)."
    },
    "comments": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 5
      },
      "description": "List of specific, actionable review comments."
    }
  },
  "additionalProperties": false
}
